version: 2

models:
  - name: fct_despesas_por_orgao
    description: "Despesas agregadas por órgão/secretaria, lidas da tabela raw do portal e com cast numérico nos valores financeiros"
    columns:
      - name: empresa
        data_type: text
        tests:
          - not_null
      - name: ano
        data_type: integer
        tests:
          - not_null
      - name: codigo
        data_type: text
      - name: descricao
        data_type: text
      - name: empenhado
        data_type: numeric(15, 2)
      - name: liquidado
        data_type: numeric(15, 2)
      - name: pago
        data_type: numeric(15, 2)
      - name: dotacao_atualizada
        data_type: numeric(15, 2)
